package com.syednoufal.rideflow.navigation

/** Route constants owned by the app graph itself, as opposed to a feature module's own routes. */
object AppRoutes {
    const val PROFILE = "app/profile"
}
